<Window x:Class="Stolovayaa.Views.MainWindow"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Школьная столовая" 
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#E8F0FE">

    <Grid>
        <Border Background="White" 
                CornerRadius="15" 
                Margin="20"
                Padding="30">

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="🍽️" 
                           FontSize="72" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>

                <TextBlock Text="Школьная столовая" 
                           FontSize="36" 
                           FontWeight="Bold"
                           Foreground="#2C3E50"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,15"/>

                <TextBlock Text="Добро пожаловать!" 
                           FontSize="20" 
                           Foreground="#7F8C8D"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30"/>

                <Border Background="#E8F8F5" 
                        CornerRadius="10" 
                        Padding="15" 
                        Margin="0,0,0,30">
                    <TextBlock Text="✅ Вы успешно вошли в систему" 
                               FontSize="16" 
                               Foreground="#27AE60"
                               HorizontalAlignment="Center"/>
                </Border>

                <Button x:Name="LogoutButton" Content="Выйти из системы"
                        Background="#E74C3C"
                        Foreground="White"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Height="45"
                        Width="200"
                        Cursor="Hand"
                        Click="LogoutButton_Clickkk">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="25">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#C0392B"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>